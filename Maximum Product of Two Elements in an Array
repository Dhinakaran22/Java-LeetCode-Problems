class Solution {
    public int maxProduct(int[] nums) {
        Arrays.sort(nums);
        int sum = 0;
        for(int i=0;i<nums.length;i++) {
            int a = nums[nums.length-2] - 1;
            int b = nums[nums.length-1] - 1;
            sum = a * b;
        }
    return sum;
    }
}
